require 'spec_helper'

describe Debito do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Debito.create!(@valid_attributes)
  end
end
